/**
* Main Application functions and objects
*/


// if it is not loaded globally via wordpress intitiate it
var fbas = window.fbas || {};
window.fbas = fbas;

// application specific scope:
( function( _, $, Backbone ){

	// setup application models
fbas.album


} ( _ , jQuery, Backbone ) ); // end model 